mixin blocklinks()
    ul.blocklinks
        block

mixin blocklink(url, isActive)
    li.blocklink(class= isActive ? 'active' : '')
        a(href=url)
            span.filler
        .content
            block

